# 📘 คู่มือแนะนำและติดตั้งระบบ IPQC Web Application (สำหรับผู้เริ่มต้น)

คู่มือฉบับนี้จัดทำขึ้นเพื่อให้ผู้ที่มารับช่วงต่อ หรือผู้ที่สนใจ สามารถเข้าใจโครงสร้างการทำงานของระบบ **IPQC Web Application (Dispensing, Laser, POF, Damper)** ได้อย่างรวดเร็ว และสามารถตั้งค่า (Setup) ระบบเพื่อรันบนเครื่อง Server หรือเครื่องส่วนตัวได้อย่างถูกต้องแบบ Step-by-Step ครับ

---

## 🏗️ 1. สถาปัตยกรรมของระบบ (System Architecture)
ระบบนี้ถูกออกแบบมาในรูปแบบ **Web Application (Client-Server Architecture)** โดยแบ่งการทำงานออกเป็น 3 ส่วนหลัก (3-Tier) ดังนี้:

### 🖥️ 1. Frontend (ส่วนหน้าบ้าน)
- **เครื่องมือที่ใช้:** HTML5, CSS3 (Vanilla), JavaScript (ES6)
- **หน้าที่:** เป็นหน้าจอส่วนติดต่อผู้ใช้ (UI) สำหรับให้พนักงาน QC กรอกข้อมูล, คำนวณสถิติเบื้องต้นแบบ Real-time, แจ้งเตือนสีแดง/เขียว (Pass/Fail) และมีระบบ Save Draft ลงใน `localStorage` ของเบราว์เซอร์เพื่อป้องกันเน็ตหลุด

### ⚙️ 2. Backend (ส่วนหลังบ้าน / API)
- **เครื่องมือที่ใช้:** **Node.js** (รันบน Express.js Framework)
- **หน้าที่:** เป็นตัวกลางคอยรับข้อมูลจากหน้าเว็บ (Frontend) มาตรวจสอบความถูกต้อง และใช้เชื่อมต่อไปยังฐานข้อมูล (Database) โดยจะประมวลผลการทำงานแบบ Batch Transaction (บันทึกทีละหลายรายการพร้อมกัน)

### 🗄️ 3. Database & Web Server (ฐานข้อมูลและเซิร์ฟเวอร์)
- **เครื่องมือที่ใช้:** **XAMPP** (รวม Apache Web Server, MySQL และ phpMyAdmin)
- **หน้าที่:** 
  - **MySQL:** เป็นฐานข้อมูลหลักสำหรับเก็บข้อมูล Record ชิ้นงานทั้งหมดที่พนักงาน QC บันทึกมา รวมถึงการตั้งค่า Spec ต่างๆ ของ Product (System Config)
  - **phpMyAdmin:** เป็นหน้าต่างเว็บสำหรับให้แอดมินหรือโปรแกรมเมอร์เข้าไปจัดการฐานข้อมูล MySQL ได้ง่ายๆ แบบคลิกแทนการพิมพ์คำสั่ง (GUI)
  - **Apache:** ทำหน้าที่เป็น Web Server สำหรับเปิดให้เครื่องอื่นๆ ในวง LAN สามารถเข้ามาใช้งานหน้าเว็บ Frontend ได้

---

> [!NOTE]
> **สรุป Flow การทำงานง่ายๆ:** 
> พนักงานกรอกหน้าเว็บ (Frontend/Apache) ➡️ กดบันทึก ➡️ ข้อมูลส่งไปหา Node.js (Backend) ➡️ Node.js เซฟข้อมูลลง MySQL (Database)

---

## 🛠️ 2. การเตรียมความพร้อมก่อนติดตั้ง (Prerequisites)

ก่อนที่จะรันระบบได้ เครื่อง Server หรือเครื่องคอมพิวเตอร์ของคุณจำเป็นต้องมี 2 โปรแกรมนี้:

1. **XAMPP:** ใช้สำหรับรันเซิร์ฟเวอร์จำลอง (Apache) และฐานข้อมูล (MySQL)
   - 📥 ดาวน์โหลด: [https://www.apachefriends.org/](https://www.apachefriends.org/)
2. **Node.js:** ใช้สำหรับรันระบบ Backend API
   - 📥 ดาวน์โหลด: [https://nodejs.org/](https://nodejs.org/) (แนะนำเวอร์ชัน LTS)

---

## 🚀 3. ขั้นตอนการติดตั้งและรันระบบ (Step-by-Step Guide)

### ขั้นตอนที่ 1: ตั้งค่าและเปิดใช้งาน XAMPP
1. ติดตั้ง XAMPP ให้เรียบร้อย และเปิดโปรแกรม **XAMPP Control Panel** ขึ้นมา
2. กดปุ่ม **Start** ที่บรรทัด **Apache** (Web Server)
3. กดปุ่ม **Start** ที่บรรทัด **MySQL** (Database)
4. สังเกตว่าตัวหนังสือ Apache และ MySQL จะกลายเป็นแถบสีเขียว ถือว่าเซิร์ฟเวอร์พร้อมใช้งานแล้ว

### ขั้นตอนที่ 2: นำโค้ด Frontend ไปวางบน Server
1. นำไฟล์โค้ดส่วนหน้าบ้านทั้งหมด (เช่น `dispensing.html`, `dispensing.js`, ไฟล์ CSS ฯลฯ)
2. นำไปวางไว้ในโฟลเดอร์ `htdocs` ของ XAMPP (ปกติจะอยู่ที่ `C:\xampp\htdocs\ipqc_system`)
3. ทดสอบเข้าหน้าเว็บผ่านเบราว์เซอร์ โดยพิมพ์ URL: `http://localhost/ipqc_system/dispensing.html` (หรือถ้าใช้เครื่องอื่นเข้า ให้เปลี่ยน localhost เป็น IP Address ของเครื่อง Server เช่น `http://192.168.1.10/ipqc_system...`)

### ขั้นตอนที่ 3: สร้างฐานข้อมูลด้วย phpMyAdmin
1. เปิดเบราว์เซอร์ พิมพ์ URL: `http://localhost/phpmyadmin`
2. ระบบจะพาเข้าสู่หน้าต่างบริหารจัดการฐานข้อมูล
3. คลิกเมนู **"New"** ทางแถบซ้ายมือ เพื่อสร้าง Database ใหม่
4. ตั้งชื่อ Database เช่น `belton_ipqc` และเลือก Collation เป็น `utf8mb4_general_ci` (เพื่อให้รองรับภาษาไทยได้ดี) แล้วกด **Create**
5. นำไฟล์โครงสร้างฐานข้อมูล (ปกติจะเป็นไฟล์สกุล `.sql` ที่ทีมพัฒนา Export ไว้ให้) นำมา **Import** เข้าสู่ Database ที่เพิ่งสร้าง เพื่อสร้างตารางต่างๆ (Tables) ให้พร้อมใช้งาน

### ขั้นตอนที่ 4: ตั้งค่าและเปิดใช้งาน Backend (Node.js)
1. เข้าไปที่โฟลเดอร์ของ **Backend API** (โฟลเดอร์ที่มีไฟล์ `server.js` หรือ `app.js` และไฟล์ `package.json`)
2. คลิกขวาที่พื้นที่ว่างในโฟลเดอร์ เลือก **Open in Terminal** (หรือเปิด Command Prompt แล้ว `cd` มาที่โฟลเดอร์นี้)
3. พิมพ์คำสั่งติดตั้งไลบรารีที่จำเป็น (ทำครั้งแรกครั้งเดียว):
   ```bash
   npm install
   ```
4. เปิดไฟล์การตั้งค่าการเชื่อมต่อฐานข้อมูล (เช่น `config.js` หรือ `db.js`) แล้วตรวจสอบให้แน่ใจว่า:
   - **Host:** `localhost` (หรือ IP ของเซิร์ฟเวอร์)
   - **User:** `root` (ค่าเริ่มต้นของ XAMPP มักจะไม่มีรหัสผ่าน)
   - **Password:** ` ` (ปล่อยว่างไว้หากไม่ได้ตั้งรหัส)
   - **Database:** `belton_ipqc` (ชื่อฐานข้อมูลที่สร้างในขั้นตอนที่ 3)
5. พิมพ์คำสั่งเปิดการทำงานของ Backend API:
   ```bash
   node server.js
   ```
   *(หรือถ้ามี Nodemon สามารถใช้ `nodemon server.js` เพื่อให้เซิร์ฟเวอร์รีสตาร์ทตัวเองเวลามีการแก้โค้ดได้)*
6. หากคอนโซลขึ้นข้อความว่า `Server running on port ...` และ `Connected to MySQL...` แสดงว่า Backend พร้อมทำงานแล้ว!

---

## 🎯 4. วิธีการดูแลรักษาระบบ (Maintenance & Troubleshooting)

> [!WARNING]
> **ปัญหาที่พบบ่อย (Common Issues)**

1. **ปัญหา: พนักงานกดเซฟข้อมูลในหน้าเว็บ แล้วระบบขึ้นว่า ❌ "Offline Mode" หรือ "เชื่อมต่อ Server ไม่ได้"**
   - **สาเหตุ:** Backend (Node.js) อาจจะดับ หรือปิดตัวลง
   - **วิธีแก้:** ให้ไปที่เครื่อง Server ตรวจสอบหน้าจอ Terminal ว่า Node.js ดับไปหรือไม่ หากดับให้พิมพ์คำสั่ง `node server.js` เพื่อรันใหม่ 
   - *(แนะนำ: บนเซิร์ฟเวอร์จริง ควรใช้โปรแกรมอย่าง `PM2` ในการรัน Node.js เพื่อให้มันทำงานเป็น Background Service และเปิดตัวเองอัตโนมัติหากเครื่อง Restart)*

2. **ปัญหา: เข้าหน้าเว็บ `http://localhost/...` ไม่ได้ หรือขึ้น Site can't be reached**
   - **สาเหตุ:** XAMPP (Apache) ไม่ได้เปิดการทำงาน
   - **วิธีแก้:** เปิด XAMPP Control Panel และกด Start ตัว Apache ใหม่อีกครั้ง

3. **ปัญหา: ข้อมูลหน้าเว็บโหลดไม่ขึ้น (Product หาย หรือดึงข้อมูลเก่าไม่ได้)**
   - **สาเหตุ:** ขาดการเชื่อมต่อกับ Database หรือ MySQL Service ใน XAMPP ดับ
   - **วิธีแก้:** ตรวจสอบ XAMPP ว่าตัว MySQL เป็นสีเขียวอยู่หรือไม่ ถ้าไม่ ให้กด Start ใหม่ 

4. **ปัญหา: Port ชนกัน (เช่น กด Start Apache ใน XAMPP แล้วเด้ง Error สีแดง)**
   - **สาเหตุ:** พอร์ตหมายเลข 80 หรือ 443 ถูกโปรแกรมอื่นแย่งใช้ไปแล้ว (เช่น Skype, IIS)
   - **วิธีแก้:** เปิด XAMPP > ไปที่ Config ของ Apache > แก้ไขไฟล์ `httpd.conf` เปลี่ยนเลขพอร์ตจาก 80 เป็น 8080 เป็นต้น (จากนั้นเวลาเข้าเว็บต้องพิมพ์ `http://localhost:8080/`)

---

## 📂 สรุปบทบาทของเทคโนโลยีที่เลือกใช้ (Why these tech stacks?)

- **HTML/CSS/JS (Frontend):** เพราะยืดหยุ่นสูง สามารถรันบนเบราว์เซอร์ได้ทุกเครื่องบนไลน์การผลิตโดยไม่ต้องติดตั้งโปรแกรมเพิ่ม
- **XAMPP (Apache + MySQL):** เพราะเป็นแพ็กเกจที่ติดตั้งง่าย ครบจบในตัวเดียว เหมาะกับการนำมาทำเป็น Web Server ภายในวง LAN ของโรงงาน
- **Node.js:** เพราะรองรับการเขียนโค้ดด้วยภาษา JavaScript เหมือนฝั่งหน้าบ้าน (Frontend) ทำให้โปรแกรมเมอร์ดูแลง่าย และเด่นเรื่องการทำระบบแบบ Non-blocking I/O ซึ่งเหมาะกับการรับข้อมูลหลายๆ เครื่องพร้อมกัน
- **phpMyAdmin:** เพราะช่วยให้ผู้ดูแลระบบ (Admin) เข้าไปดูตารางข้อมูล ดึง Report หรือแก้ไขข้อมูลที่ผิดพลาดได้ง่ายผ่านเว็บ โดยไม่ต้องพิมพ์คำสั่ง SQL ด้วยตัวเอง
